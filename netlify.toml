[build]
  # Build command runs npm build script
  command = "npm run build:web"
  # Publish the dist folder generated by Expo
  publish = "dist"
  # Serverless functions
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
